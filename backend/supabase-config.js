/* ============================================================
   Fill these in once your Supabase project exists.
   Dashboard → Settings → API → "Project URL" and "anon public" key.

   Safe to expose in client-side code — the anon key only grants
   what your Row Level Security policies (schema.sql) allow. It is
   NOT the same as the service_role key, which must never appear
   in this app or any other browser-shipped file.
   ============================================================ */

const SUPABASE_URL = "https://cxeibgrrrdvxadaxmxrw.supabase.co";
const SUPABASE_ANON_KEY = "sb_publishable_LYj5zoiEzfj-YCBr1PvM8A_hikDTFXP";
